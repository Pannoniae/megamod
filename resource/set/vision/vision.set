{visions
	(define "radius"
		(mod "mp" {radius (* 0.75 %r)})		; // This value changes the global vision ranges of the game.
		; decrease it!
		(mod not "mp" {radius (* 0.75 %r)})  ; // This value changes the global vision ranges of the game.
	)
	(include "spotting.inc")
	(include "hearing.inc")
	(include "vision_fields.inc")
}
