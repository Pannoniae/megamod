{game_entity
	(include "/properties/tank.ext")
	(include "/properties/shadow/tank.inc" scale(1.0))
	(include "/properties/selection/vehicle.inc" scale(0.9))
	{collider "tank.light"}
	{props "light" "vision_lev03" "detect_tank_mg" "smoke" "4in" "crusader_mki"}
	{targetclass "tank_light"}
	{targetSelector "tank_bullet"}
	{patherId "panzer1"}
	{collider "tank.light"}

	;{extender "enumerator"}

	("tank_light_tier1")
	("tracks" repair(30) thickness(20))
	("gun" repair(20) thickness(15))
	
	{Volume "Body"
		{thickness 60
			{front 60}	
			{top 12}			
			{rear 20}
			{bottom 12}
		}
	}
	{Volume "mantlet"
		{tags "turret"}
		{component "turret"}
		{thickness 60}
	}
	{Volume "turret"
		{thickness 60
			{top 12}			
			{bottom 12}
		}
	}
	{Volume "engine"
		{thickness 10}
	}
	{extender "shield"
		{on}
	}
		
	("shield" args "shieldl1" 8)
	("shield" args "shieldr1" 8)

	("crew_2_human")
	{boarder
		{anm "left"
			{forward	{begin	"board_tank_left"}}
			{reverse 	{end	"emit_tank_1_hold"}}
		}
		{anm "right"
			{forward	{begin	"board_tank_right"}}
			{reverse 	{end	"emit_tank_1_hold"}}
		}
		{anm "armor"
			{forward	{begin	"board_LowArmor"} 	{end "seat_armor"}}
			{reverse 	{end	"emit_tank_1"}}
		}
	}
	{sensor
		{visor "main"
			{bone "foresight14"}
		}
	}
	{extender "smokescreen"
		{cooldown 20}
		{itemTags "no8_smoke"}
	}
	{extender "inventory"
		{box
			{item "dynamite"}
			{item "ammo 13x81" 1100}
			{item "no8_smoke" 2}
		}
	}
	{Weaponry
		("restore_ik_time")
		{place "mgun1"
			{RestoreIKAfterAim}
			{type "main"}
			{weapon "13mm_vickers50" filled}
			{foresight "foresight4"}
			{charger "gunner"}
			("abm_mgun")
		}
	}
	{mass 11200}
	{Chassis
		("tank_trace" fx("tracks_sm2") step(0.15) len(0.6))
		("mobility_tank"
			speed(13) ;----------Max speed when fully accelerated on road, km/h.
			reverse(3) ;---------Max speed in reverse on road, km/h.
			traverse(32) ;-------Track traverse, per second, in degrees.
			weight(11.2) ;--------Weight in Tonnes.
			power(70) ;---------Engine Power, in hp.
			track(4) ;-----------Track performance: (0,10], 10=ideal. Sluggish tanks and vehicles with narrow and non-gripping tracks have a lower performance.
			fuel(146) ;----------Fuel capacity in Litres.
			type(fuel) ;---------Fuel type. (diesel OR fuel)
			range(128) ;---------Range in km, off-road.
		)
	}
	{Extension "a11_matilda.mdl"}
	{bone "gun_rot"
		{speed2 15}
	}
	{bone "turret"
		{speed2 20}
	}
}

