{game_entity
	(include "/properties/cannon.ext")
	(include "/properties/shadow/tank.inc" scale(0.6))
	{Extension "47mm_type1.mdl"}
	(include "/properties/selection/cannon.inc" scale(0.7))
	{props "cannon_small" "wheeled" "vision_lev02_can" "muzzle_37mm_47mm" "cannon_sandbag_small" "trailer" "trailer_light" "reset" "detect_atgun_small"}
	{collider "cannon_small"}
	{targetclass "cannon_small"}
	{targetSelector "cannon_small"}

	("wheel" args "wheelright")
	("wheel" args "wheelleft")

	("gun" repair(30) thickness(20))

	("armor_light_cannon")
	("crew_wheeled_cannon_4")

	{boarder
		{anm "gunner"
			{forward {end "pose_new_cannon_gunner_fire" 10}}
		}
		{anm "charger"
			{forward {end "pose_new_cannon_charger_fire" 10}}
		}
		{anm "charger2"
			{forward {end "pose_new_cannon_charger2_fire" 10}}
		}
		{anm "commander"
			{forward {end "pose_new_cannon_commander_fire" 10}}
		}
		{anm "driver1"	;// gunner
			{forward {end "cannon_pak40_pusher_left" 10}}
		}
		{anm "driver2"	;// charger
			{forward {end "cannon_pak40_pusher_right" 10}}
		}
		{anm "driver3"	;// charger2
			{forward {end "mortar_driver_l" 10}}
		}
		{anm "driver4"	;// commander
			{forward {end "mgun_browning_driver_r" 10}}
		}
		{anm "seat1"	;// gunner
			{forward {end "pose_seat_cannon_left" 10}}
		}
		{anm "seat2"	;// charger
			{forward {end "pose_seat_cannon_right" 10}}
		}
		{anm "seat3"	;// charger2
			{forward {end "pose_seat_cannon_left" 10}}
		}
		{anm "seat4"	;// commander
			{forward {end "pose_seat_cannon_right" 10}}
		}
	}

	{extender "cannon"
		{animation
			{switch "close"}
			;// "gunner" "charger" "charger2" "commander"
			{move "cannon_pak40_pusher_left" "cannon_pak40_pusher_right" "mortar_driver_l" "mgun_browning_driver_r" }
			{aim "cannon_m42_gunlayer" "" "" ""}
			{fire "new_cannon_gunner_fire" "new_cannon_charger_fire" "new_cannon_charger2_fire" "new_cannon_commander_fire"}
			{reload "new_cannon_gunner_reload" "new_cannon_charger_reload" "new_cannon_charger2_reload" "new_cannon_commander_reload"}
		}
		{move {workers "driver1" "driver2" "driver3" "driver4" minWorkers 2} {obstacle "close"}}			;// move state: cannon being pushed and which positions play animations
		{attack {workers "gunner" "charger" "charger2" "commander"} {obstacle "open"}}		;// fire/attack state: cannon aim, fire, and reload events
		{trailing {workers "seat1" "seat2" "seat3" "seat4"}}	;// trailing state: cannon is being towed {obstacle} not needed. Uses "close"
	}
	{extender "inventory"
		{box
			{item "bullet47_jap aphe" 50}
			{item "bullet47_jap he" 5}
			{item "sandbag_vehicle" 1}
		}
	}
	{Weaponry
		("restore_ik_time")
		{place "gun"
			{weapon "47mm_type1" filling "ammo bullet47_jap aphe" 1}
			{RestoreIKAfterAim}
			("abm_dymamic_gun"
				zeroing(1.25)
				dispersion(0.2)
			)
		}
	}
	{mass 754}
	{Chassis "track"
		("car_trace" fx("tracewheel05a") step(0.14) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("mobility_cannon"
			speed(7.8) ;----------------------------------Max speed.
			weight(754) ;---------------------------------Weight in Kilograms.
		)
	}

	{bone "basis"
		{limits}
		{speed2 9}
	}
	{bone "gun_rot"
		{speed2 10}
	}
	{bone "turret"
		{limits -20 20}
		{speed2 18}
	}
}