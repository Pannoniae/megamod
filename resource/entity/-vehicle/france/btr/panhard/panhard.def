{game_entity
	(include "/properties/armoredcar.ext")
	(include "/properties/shadow/tank.inc" scale(1.0))
	(include "/properties/selection/vehicle.inc" scale(1.0))
	{props "tank" "turret" "wheeled" "with_gun" "vision_lev02" "muzzle_25mm"}
	{Extension "panhard.mdl"}
	{extension "crew.ext"}
	(include "panhard.ext")
	{targetclass "armoredcar"}
	{targetSelector "tank_light"}

	{extender "inventory"
		{box
		       	{item "ammo 7,5x54" 2750}
                   	{item "bullet25_fra ap" 47}
		}
	}
	{Weaponry
            	("restore_ik_time")
		{place "gun"
			{RestoreIKAfterAim}
			{weapon "25mm_sa35" filling "bullet25_fra ap"}
			{gunner "gunner"}
			{charger "charger"}
			("abm_dymamic_stabilised"
				zeroing(2.8)
				dispersion(0.20)
			)
		}
		{place "mgun1"
                  	{type "coaxial"}
			{weapon "reibel" filled}
			{gunner "gunner"}
			{charger "charger"}
			{foresight "foresight4"}
			{basic}
			("abm_mgun")
		}
	}	
	{mass 8200}
	{Chassis "wheel"
		("car_trace" fx("tracewheel03") step(0.08) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("car_trace" fx("tracewheel03") step(0.08) wheell("wlr") wheelr("wrr") bonel("FXtraceL2") boner("FXtraceR2"))
		("mobility_car"
			speed(72) ;----------------------------------Max speed.
			speedatmaxturn(17) ;-------------------------Max speed at max turn.
			reverse(30) ;--------------------------------Max speed in Reverse.
			weight(8.2) ;--------------------------------Weight in Tonnes.
			power(105) ;---------------------------------Horse Power.
			fuel(145) ;----------------------------------Fuel capacity in Litres.
			type(fuel) ;---------------------------------Fuel type.
			range(200) ;---------------------------------Range in Kilometers.
		)
	}

	{bone "gun_rot"
		{speed2 10}
	}
	{bone "turret"
		{speed2 15}
	}
}